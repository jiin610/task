| 전공   | 학번         | 이름  |
| :--- | :--------- | :-- |
| 통계학과 | 2024004917 | 정지인 |
## 1. 프로젝트 이름
- 나만의 빈출 단어장
- 부제: 텍스트 데이터 분석을 통한 효율적인 외국어 학습 도우미

## 2. 프로젝트 한줄 소개
> 사용자가 입력한 실제 텍스트 데이터를 분석하여, 나에게 꼭 필요한 핵심 단어만 추출해 주는 **맞춤형 단어장 생성 프로그램**

## 3. 기획 배경 및 개발 목적
- 문제 인식
	- 외국어 학습 시 어떤 단어부터 외워야 할지 우선순위를 정하기 어려움
	- 가사, SNS 게시글, 원서 등 실제 텍스트에서 단어를 추출, 정리하는데 과다한 시간 소요
- 개발 목적
	- **학습 효율 극대화**: 실제 텍스트 기반의 고빈도 단어를 우선적 학습하여 실전 감각 향상
	- **단어장 생성 자동화**: 단어 추출 및 정렬 과정을 자동화하여 학습 시간 절약
- 예상 사용자
	- 단어를 효율적으로 학습하고 싶은 중국어(번체) 초보

## 4. 핵심 기능 
- 1️. **텍스트 전처리**
	- 입력된 텍스트의 언어 자동 감지
	- 불필요한 특수기호 및 공백 제거를 통한 분석용 데이터 정제
- 2️. **빈도 통계 분석**
	- 단어별 등장 횟수 및 상대 빈도(%) 산출
	- 출현 빈도를 기준으로 한 내림차순 순위 정렬
- 3️. **맞춤형 단어장 출력**
	- 학습자가 지정한 상위 N개의 고빈도 단어 추출
	- 접근성이 높은 CSV 파일 형태로 자동 저장 및 출력

## 5. 예상 형태
- CLI
- 이 프로그램의 핵심 기능은 화면에 무언가를 보여주는 것이 아니라 분석 결과를 CSV 파일로 저장하는 것, 따라서 별도의 GUI를 설계할 필요 없이, 사용자가 몇 가지 질문에 답하면 파일이 생성되는 CLI 형태가 이 프로그램의 목적에 부합
-----
## 6. 프로그램 작성 과정
#### 초기 코드
- **핵심 로직:** 분석할 단일 텍스트 파일의 경로를 입력받아 데이터를 읽어오는 기본 구조
    
- **적용 기술:** 띄어쓰기가 없는 언어(중국어 번체자 등)의 분석을 위해 형태소 분석 라이브러리(`jieba`) 및 deep-translator 라이브러리로 Google 번역 API를 호출
###### 결과물(실제사용예시)
<img width="4096" height="4096" alt="image" src="https://github.com/user-attachments/assets/e32ab013-ba84-41de-840f-f09869696385" />
<img width="2631" height="1280" alt="image" src="https://github.com/user-attachments/assets/007a568d-fce2-4bfd-ab72-9673fb2ca552" />
<img width="1280" height="1935" alt="image" src="https://github.com/user-attachments/assets/3b835af3-187a-4bc0-ad67-58fef5cedb4e" />

###### 한계점
- **작업 비효율성:** 다량의 텍스트를 분석하고자 할 때 매번 단일 파일만 입력하고 프로그램을 재실행해야 하는 번거로움 존재
    
- **통계 왜곡:** 노래 후렴구 등 중복 구절이 그대로 집계되어 특정 단어의 빈도수가 비정상적으로 높게 측정되는 현상 발생
    
- **분석 목적 퇴색:** 단일 파일 분석만으로는 전체 문서를 관통하는 평균 빈도를 산출할 수 없어 보편적인 핵심 단어 추출에 한계가 있음
    
- **번역 품질 저하 및 오역:** 외부 기계 번역 API 연동 시, 문맥을 고려하지 못하고 단어를 직역함에 따라 잦은 오역이 발생하고 전체적인 데이터 품질과 단어장의 신뢰성이 떨어지는 문제 직면

#### 최종 코드
###### 개선 아이디어
- **다중 파일 통합 지원:** 쉼표(,)를 통해 여러 개의 텍스트 파일을 한 번에 입력받고 통합 분석하는 로직을 구현하여 작업 효율성 대폭 향상
    
- **중복 구절 필터링 추가:** 데이터 전처리(Data Preprocessing) 단계에서 중복되는 문장을 사용자의 선택에 따라 제거하여 무의미한 빈도수 왜곡 방지
    
- **기계 번역 기능 제거:** 잦은 오역과 번역 품질 저하로 인한 학습의 혼란을 막고, 원문 데이터의 정밀한 빈도 통계 도출이라는 본 프로젝트의 본질적 목표에 집중
***→대신, 프로그램이 1차로 뽑아준 핵심 단어 데이터를 대화형 AI에게 전달하여, 학습자가 공부하기 가장 완벽한 형태의 맞춤형 해설지를 만들어내는 하이브리드 방식을 기획***

    
- **파일당 평균 등장 횟수 도출:** 누적 빈도를 유효 파일 개수로 나누는 파생 변수 컬럼을 추가하여, 특정 문서에 편향되지 않은 범용적인 '핵심 단어' 지표 제공

###### 결과물(실제 사용 예시)
<img width="4096" height="2497" alt="image" src="https://github.com/user-attachments/assets/9bfbc91e-5a5b-4fc7-974e-7452ef4a65f4" />
<img width="1059" height="678" alt="image" src="https://github.com/user-attachments/assets/f0b53857-4414-4c8b-8045-c0d20709cf6e" />
<img width="844" height="354" alt="image" src="https://github.com/user-attachments/assets/ebd5d29f-9e7e-4b37-9ec8-6c50912bc3ea" />
<img width="510" height="571" alt="image" src="https://github.com/user-attachments/assets/82b75c3d-7bb0-43ad-9b0d-38e44be307a5" />



#### 1. 맞춤형 이원화 암기 자료 확보 (테스트용 & 정답용)

- **활용 방식:** 프로그램이 1차로 정확하게 추출해 낸 '뜻이 적혀있지 않은 원본 단어장(CSV)'과, 이를 AI에게 넘겨 검수받은 **'뜻과 예문이 포함된 단어장'** 두 가지 버전을 모두 손쉽게 확보
    
- **학습 효과:** 뜻이 없는 원본 파일은 내 암기 상태를 스스로 점검하는 '셀프 테스트용'으로, 뜻이 포함된 파일은 '정답 확인 및 해설지'로 병행 활용하여 훨씬 입체적이고 능동적인 복습 가능
    

#### 2. 즉각적인 학습 효능감 체험 (학습 지속성)

- **활용 방식:** 단어만 외우고 끝내는 일회성 학습이 아니라, 단어장 추출의 기반이 되었던 원문 텍스트 파일(노래 가사, 기사 등)을 핵심 독해 자료로 100% 재활용 가능
    
- **학습 효과:** 단어장을 공부하기 전과 후, 동일한 원문을 다시 읽어보며 스스로 해석할 수 있는 문장이 늘어나는 것을 눈으로 직접 확인하게 된다. 이러한 즉각적인 성취감(학습 효능감)은 외국어 공부의 재미를 붙여주고 장기적으로 공부를 지속할 수 있는 강력한 원동력으로 작용
    

#### 3. 반복적인 프롬프트 작성 없는 시간 단축 (효율성 극대화)

- **활용 방식:** 매번 새로운 텍스트를 공부할 때마다 AI에게 프롬프트를 일일이 쓸 필요 없음
    
- **효율성 효과:** AI에게 명령어를 고민하고 입력하는 소모적인 작업 시간과 피로감을 줄여준다. AI에게는 오직 '최종 정리된 핵심 단어들의 검수와 뜻 달기'라는 가장 본질적인 역할만 요청하면 되므로, 전체적인 학습 준비 프로세스가 극도로 간결해짐

-----

## 8. 프로젝트에서 AI 활용법

- **1. 프로젝트에 필요한 패키지 탐색 및 추천**
	 중국어 텍스트 분석이라는 목표를 설명했을 때, AI가 형태소 분석에 적합한 `jieba`, 번역에 적합한 `deep-translator` 패키지를 추천
- **2. 초보 수준의 코드를 효율적으로 개선**
	 AI의 도움을 받아 기초적인 코드를 더 간결하고 안전한 구조로 발전

**개선 전 — 파일 하나씩 직접 처리**
```python
file_path = input("파일명 입력: ")
with open(file_path, 'r') as f:
    text = f.read()
words = text.split()
word_counts = {}
for word in words:
    if word in word_counts:
        word_counts[word] += 1
    else:
        word_counts[word] = 1
```
**개선 후 — 다중 파일, Counter, 예외 처리 적용**

```python
file_paths = [f.strip() for f in file_input.split(',')]
total_word_counts = Counter()

for file_path in file_paths:
    if not os.path.exists(file_path):
        print(f"⚠️ '{file_path}' 파일을 찾을 수 없어 분석에서 제외합니다.")
        continue
    try:
        with open(file_path, 'r', encoding='utf-8') as f:
            text = f.read()
        total_word_counts.update(jieba.lcut(text))
    except Exception as e:
        print(f"❌ 오류 발생: {e}")
```

- **3. 오류 원인 분석 및 즉각적인 디버깅**
	 코드 실행 중 발생한 오류를 AI에게 그대로 붙여넣으면 원인과 해결 방법을 바로 제시
	 예를 들어 `PermissionError`가 발생했을 때 "CSV 파일이 엑셀에 열려 있어서 덮어쓰기가 막힌 것"이라는 원인을 파악하고, 타임스탬프를 파일명에 추가해 충돌을 원천 차단하는방식으로 해결
	 오류 메시지를 해석하는 능력이 부족한 초보 개발자에게 특히 유용

## 9. 어려웠던 점 / 배운 점
사용자 입장에서 최적화된 프로그램을 만들기 위해 끊임없이 직접 사용해보고 개선하는 과정을 반복했고, 그 과정이 개발 시간의 대부분을 차지했으며, 동시에 가장 어려웠던 부분이다.
아래는 그 과정에서 고민했던 주요 지점들이다.

- 단어장 출력 기본값을 20개로 설정한 것은 임의의 수치가 아니라, 직접 단어를 외워보며 한 번에 학습하기에 부담 없는 양을 고민한 끝에 내린 결론이다.
- 텍스트를 파일로 저장한 뒤 처리하는 방식은 가사나 SNS 글 등 학습 자료를 꾸준히 축적하고 나중에 재활용할 수 있도록 고안한 구조이다.
- 쉼표 구분 방식의 번거로움, 그리고 파일 수가 많아야 분석 품질이 올라간다는 점도 고민거리였다. 그러나 파일을 무조건 늘리는 것은 사용자 맞춤이라는 목적에 부합하지 않을 수 있고, 그만큼 많은 예문을 소화할 수 있는 사용자는 이미 초보 학습자와는 거리가 멀다고 판단해 현재 방식을 유지하기로 했다.

이번 프로젝트를 통해 좋은 프로그램이란 단순히 오류 없이 실행되는 것이 아니라, 사용자가 실제로 편하게 쓸 수 있어야 한다는 것을 배웠다. 개발자 입장에서는 완성된 것처럼 보여도 직접 사용해보면 불편한 점이 반드시 생겼고, 그 간극을 좁히는 과정이 개발의 핵심임을 깨달았다.
또한 처음부터 완벽한 설계를 목표로 하기보다, 일단 작동하는 것을 만들고 실제로 써보며 개선해나가는 방식이 훨씬 효율적이라는 것도 배웠다. 기능을 추가할 때마다 "이게 정말 사용자에게 필요한가"를 먼저 생각하는 습관이 자연스럽게 생겼다.

## 10. 향후 목표
현재는 중국어(번체) 텍스트에 특화되어 있지만, 향후 영어 등 다른 언어로도 확장해 언어에 관계없이 사용할 수 있는 범용 단어장 생성기를 만드는 것이 최종 목표이다. 언어별 형태소 분석기 도입 등 수정이 필요한 부분은 AI를 활용해 함께 해결해나갈 계획이며, 현재의 구조보다 더 편리한 형태가 없을지도 함께 고안해볼 예정이다.
